<main class="map-area"
      data-sidebar-target="mapArea"
      data-controller="map"
      data-map-token-value="<%= ENV.fetch("MAPBOX_TOKEN", "") %>">

  <div id="map" class="map" data-map-target="mapContainer"></div>

  <div class="map-compare" data-map-target="compareContainer" hidden>
    <div class="map-compare__left" data-map-target="compareLeft"></div>
    <div class="map-compare__right" data-map-target="compareRight"></div>
  </div>

  <div class="map-split" data-map-target="splitContainer" hidden>
    <div class="map-split__top" data-map-target="splitTop"></div>
    <div class="map-split__bottom" data-map-target="splitBottom"></div>
  </div>

  <%# Botones flotantes (derecha) %>
  <div class="map-fab">
    <button type="button" class="map-fab__btn" aria-label="Mapa">
      <i class="fa-solid fa-map"></i>
    </button>

    <button type="button"
            class="map-fab__btn"
            aria-label="Gráfico"
            data-action="click->map#toggleDashboard"
            data-map-target="dashboardBtn">
      <i class="fa-solid fa-chart-bar"></i>
    </button>

    <button type="button"
            class="map-fab__btn"
            aria-label="Leyenda"
            data-action="click->map#toggleLegend"
            data-map-target="legendBtn"
            hidden>
      <i class="fa-solid fa-list"></i>
    </button>
  </div>

  <% if user_signed_in? %>
  <div class="mode-toggle"
      data-controller="mode-toggle"
      data-mode-toggle-mode-value="constructor"
      data-sidebar-target="modeToggle"
      hidden>
    <button type="button"
            class="mode-toggle__btn"
            data-mode-toggle-target="btn"
            data-mode="constructor"
            data-action="click->mode-toggle#select">
      Constructor
    </button>

    <button type="button"
            class="mode-toggle__btn"
            data-mode-toggle-target="btn"
            data-mode="comparador"
            data-action="click->mode-toggle#select">
      Comparador
    </button>
  </div>
  <% end %>


  <div class="map-panels-right">
    <div class="map-dashboard" data-map-target="dashboardPanel" hidden>
      <div class="map-dashboard__title">Distribución por categoría</div>
      <div data-map-target="dashboardChart"></div>
    </div>

    <div class="map-legend" data-map-target="legendPanel" hidden>
      <div class="map-legend__title">Leyenda</div>
      <div class="map-legend__items" data-map-target="legendItems"></div>
    </div>
  </div>
</main>
